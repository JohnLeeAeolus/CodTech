{
  "name": "codtech-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^7.0.2"
  },
  "scripts": {
    "dev": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  }
}
